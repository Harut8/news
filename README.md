To run project:

```bash
   make run
```

To build project:

```bash
   make all
```

To migrate database:
Add model into src/core/migrations/env.py
```bash
   make revision
````

To add new dependency:

```bash
   uv add <dependency_name>
```